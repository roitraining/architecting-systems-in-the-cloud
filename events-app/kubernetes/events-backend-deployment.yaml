apiVersion: apps/v1
kind: Deployment
metadata:
  name: events-backend
  labels:
    app: events-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: events-backend
  template:
    metadata:
      labels:
        app: events-backend
    spec:
      containers:
      - name: events-backend
        image: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/events-backend:latest
        ports:
        - containerPort: 3001
        env:
        - name: PORT
          value: "3001"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: events-backend-service
spec:
  selector:
    app: events-backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP