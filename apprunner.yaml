version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building Space Invaders app"
run:
  runtime-version: latest
  command: nginx -g 'daemon off;'
  network:
    port: 80
    env: PORT
  env:
    - name: PORT
      value: "80"